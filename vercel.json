{   "version": 2,
    "builds": [
        {
            "src": "server.js connection.js chatbot.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {

            "src": "/(.*)",
            "dest": "/server.js"
        }
    ],
    "installCommand": "npm install"
}