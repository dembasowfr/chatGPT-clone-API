{
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/nodemon"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ],
    "installCommand": "npm install"
}